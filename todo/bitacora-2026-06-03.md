# Bitácora — 2026-06-03

## 01:30 — Optimización GPU-resident + sweep paralelo + resultados finales

### Cambios técnicos

- **`src/train.py`** — entrenamiento GPU-resident: el dataset del fold (~3 MB) se mueve a VRAM una sola vez y los batches se indexan en GPU, eliminando las copias RAM→VRAM por-batch del `DataLoader` y los `.item()` por-batch (que forzaban sincronización CPU↔GPU). La pérdida de época se acumula en GPU.
- **`scripts/run_parallel_sweep.sh`** (nuevo) — orquestador que lanza baseline + 9 λ con máximo 3 concurrentes (semáforo con `wait -n`), y agrega los `metrics.json` en `lambda_sweep.json` al final.
- **`scripts/06_compare_baseline_vs_pinn.py`** y **`scripts/07_plot_results.py`** — `LAMBDA_GRID` extendido a 9 valores.
- **`scripts/07_plot_results.py`** — figura de barrido rediseñada (eje log limpio, λ=0 como referencia, sin barras de error gigantes, óptimo resaltado); perfil de profundidad y crossplot en español usando λ=0.5 (operativo recomendado) sobre el pozo de mayor mejora (Soeken_12).
- **`documentation/03_baseline.md`, `04_pinn.md`, `05_methodology.md`, `README.md`** — refrescados con los números finales; nueva sección de validación externa; 3 figuras de resultados embebidas.

### Resultados finales

Baseline (λ=0): MAE=0.1396, R²=0.276, 20/27 pozos R²>0.
PINN óptimo robusto **λ=0.5**: MAE=0.1347, R²=0.327, **mejora el 81.5% de los pozos** (22/27).

**Validación externa (3 pozos ciegos) — el resultado central:**
| Pozo | MAE base→PINN | R² base→PINN |
|---|---|---|
| Arensman_2 | 0.142→0.139 | 0.385→0.391 |
| Burmeister_1 | 0.068→0.068 | 0.518→0.522 |
| Rous_'F'_2 | 0.261→0.253 | −0.204→−0.099 |
| Media | 0.157→0.153 | 0.233→0.271 |

El PINN mejora **los 3 pozos ciegos**, con la mayor ganancia en el más difícil (Rous_'F'_2). Valida la hipótesis del proyecto.

### Decisiones de diseño

- **Diagnóstico del cuello de botella**: medí que el preprocesamiento era solo el 7% del tiempo por fold (hipótesis inicial errónea). El 93% era el loop de entrenamiento con la GPU al 47% — hambrienta por el overhead por-batch, no por cómputo. El MLP (6.5k params) es tan pequeño que la GPU procesa cada batch en microsegundos y espera al CPU.
- **GPU-resident antes que paralelismo**: el paralelismo solo (3 jobs) daba 1.89× con la GPU aún al 52%. La causa real era que los datos no estaban en VRAM. Mover los datos a GPU una vez (3 MB en 16 GB, cabe 5181 veces) atacó el problema directo.
- **Combinación final**: GPU-resident (2.18×) + 3 concurrentes (GPU al 90%) = 4.85× total. El sweep de ~10h bajó a 100 min reales.
- **λ=0.5 como operativo, no λ=1.0**: λ=1.0 tiene MAE media marginalmente menor pero solo mejora el 59.3% de los pozos; λ=0.5 mejora el 81.5% capturando casi toda la ganancia. La robustez (cuántos pozos mejoran) pesa más que la media para un operativo.
- **Comportamiento monótono nuevo**: con la física mejorada (bivariate + ponderada por caliper), más λ ayuda y satura en ~0.5 — distinto al run anterior donde λ≥0.5 degradaba. La razón: el DCAL_WEIGHT apaga la física en zonas de washout, haciéndola segura de aplicar con peso alto.

### Errores

- **Hipótesis inicial errónea del cuello de botella**: asumí que el preprocesamiento (voting consensus con IsolationForest) era lo lento. Lo medí antes de optimizar: 0.40s/pozo = 7% del fold. El error habría sido cachear el preprocesamiento (esfuerzo alto, ganancia 7%) en vez de atacar el loop de entrenamiento (ganancia 4.85×). Lección: medir antes de optimizar.
- **Figura de barrido inicial inservible**: las barras de error (±std entre pozos, ~0.09) tapaban completamente la tendencia (diferencias ~0.005), y el eje symlog mostraba "λ=−10⁻²" (sin sentido). Rediseñada: la incertidumbre relevante es la tendencia de la media, no la varianza inter-pozo.
- **Figuras de resultados con λ inconsistente**: `07_plot` auto-seleccionaba el mejor λ por MAE (1.0), pero la recomendación es λ=0.5. Quedaban figuras con "PINN λ=1.0" contradiciendo el texto. Fijé `RECOMMENDED_LAMBDA=0.5` para las figuras por-pozo.

### Aprendizajes

- Para modelos minúsculos, la GPU se satura con el overhead de plomería de datos (transferencias por-batch, DataLoader), no con cómputo. Los datos residentes en VRAM + batches indexados en GPU es la palanca clave, no batches más grandes ni más capas.
- El paralelismo multi-proceso solo rinde si cada proceso ya alimenta bien la GPU. Con datos en RAM (transferencia por-batch), 3 procesos seguían dejando la GPU al 52%; con datos en VRAM, 3 procesos la llevan al 90%.
- La elección de hiperparámetro operativo debe pesar robustez (% de casos mejorados) y no solo la métrica media — la media puede premiar un λ que ayuda mucho a pocos pozos y daña a otros.

---

## 03:30 — Validación externa por dos protocolos + capítulo de resultados + diagnóstico

### Cambios técnicos

- **`src/external_eval.py`** (nuevo módulo) — centraliza la lógica de inferencia externa antes duplicada: `train_final_model` (entrena un modelo sobre los 27 pozos), `predict_well` (predicción de modelo único), `ensemble_predict` (promedio de los 27 checkpoints LOWO), `train_pool_ids` (helper de filtrado).
- **`tests/test_external_eval.py`** (nuevo, 5 tests) — shapes/finitud de `predict_well`, promedio de `ensemble_predict`, smoke + determinismo de `train_final_model`.
- **`scripts/10_external_validation.py`** (nuevo) — corre los DOS protocolos (ensemble + modelo único) para baseline y λ=0.5 sobre los 3 pozos ciegos, guarda `validation_comparison.json` y genera `external_comparison.png`.
- **`scripts/09_plot_diagnostics.py`** (nuevo esta sesión) — figuras `external_profiles.png` (3 pozos externos) e `insample_vs_oos.png` (diagnóstico modelo vs generalización). Refactorizado para usar el módulo.
- **`scripts/08_eval_external.py`** — refactorizado para usar `ensemble_predict` del módulo.
- **`documentation/06_resultados.md`** (nuevo capítulo 7) — resultados por metodología, decisión de diseño de los dos protocolos, robustez, diagnóstico, conclusiones. Cumple la tarea pendiente `04_results.md`.
- Actualizados `04_pinn.md` (§5.8.3 diagnóstico + puntero a cap. 7), `05_methodology.md` (§6.5.2 dos protocolos), `documentation/README.md` (índice + robustez).

### Resultados de validación externa (3 pozos ciegos)

| Protocolo | MAE base→PINN | R² base→PINN |
|---|---|---|
| Ensemble (27 LOWO) | 0.157→0.153 | 0.233→0.271 |
| Modelo único (27 pozos) | 0.162→0.155 | 0.171→0.257 |

El PINN mejora bajo AMBOS protocolos → ventaja robusta. La mejora es mayor en el modelo único (ΔR² +0.086 vs +0.038): la física compensa la ausencia de la reducción de varianza del ensemble.

### Decisiones de diseño

- **Dos protocolos como análisis de robustez, no como comparación de métodos.** Lo que valida el PINN es el delta λ=0→λ=0.5 dentro de cada protocolo. Reportar ambos demuestra que la ventaja no es artefacto del ensemble.
- **Módulo `external_eval` en `src/`** (no en scripts): la lógica de inferencia externa la usan 08, 09 y 10 → por convención vive en `src/` con tests.
- **Diagnóstico in-sample vs out-of-sample**: idea del usuario para distinguir si el límite es el modelo o la generalización. Resultado: el modelo ajusta lo que vio (corr 0.88 in-sample) → la arquitectura no es el cuello de botella; el reto es generalizar.

### Errores

- En `10_external_validation.py`, `_aggregate` devolvía claves sin sufijo `_mean` pero el código las accedía con `mae_mean`/`r2_mean`. Detectado en revisión antes de correr; corregido añadiendo el sufijo en la comprensión de diccionario.
- La figura inicial de perfil (Soeken_12, λ=1.0, inglés) mostraba predicciones lejanas a la real — el usuario lo notó. Causa: Soeken_12 es un pozo difícil (corr 0.49) y mala elección. Se cambió a un panel honesto (3 pozos externos) + diagnóstico in-sample, y se usó λ=0.5 consistente con la recomendación.

### Aprendizajes

- Cuando el usuario propone "graficar el set de entrenamiento", distinguir intención: como RESULTADO sería engañoso (memorización), pero como DIAGNÓSTICO (in-sample vs out-of-sample) es ciencia correcta para aislar capacidad vs generalización.
- Mostrar un resultado bajo dos protocolos de inferencia es un análisis de robustez barato (~5 min) que fortalece mucho la credibilidad de un portafolio.
- Un pozo "de máxima mejora" suele ser un pozo difícil donde ambos modelos son mediocres — mala elección para una figura que debe convencer. Para convencer, mostrar un pozo de buen ajuste (alta correlación) en datos ciegos.

---

## 20:12 — Sitio bilingüe (ES/EN) en GitHub Pages + README + sync de diagramas

### Cambios técnicos

- **`docs/` nuevo** — sitio del proyecto para GitHub Pages:
  - Sistema de diseño propio en `docs/assets/css/theme.css` (design tokens, tema claro/oscuro, componentes, math, diagramas, barra lateral colapsable).
  - KaTeX self-hosted (`docs/assets/katex/`, v0.17.0, solo woff2).
  - Flujogramas como SVG pre-renderizados con mermaid-cli en Docker; variantes claro/oscuro y ES/EN; fuentes `.mmd` + `theme_*.json` en `docs/assets/diagrams/src/`.
  - 4 páginas: `index.html`/`decisiones.html` (ES) + `index.en.html`/`decisiones.en.html` (EN). Informe de una página estilo Distill con decisiones de diseño colapsables al margen y registro exhaustivo por fase.
- **`README.md`** reescrito con datos finales (30 pozos, física bivariada + ponderación por caliper, normalización per-columna, LOWO 27, resultados reales) y enlaces a sitio + `documentation/` + paper de referencia (Pothana & Ling 2025).
- **`documentation/02_preprocessing.md` y `05_methodology.md`** — sincronizados los diagramas Mermaid con las versiones del sitio.

### Decisiones de diseño

- **i18n por dos archivos por idioma**, no toggle JS. Se construyó primero un i18n con `data-i18n` + `i18n.js` + `lang.js` y se descartó a petición del usuario: dos páginas estáticas por idioma con botón-enlace. Más simple, mejor SEO, funciona sin JS. (Guardado en memoria.)
- **Voz del texto**: primera persona, de aprendiz, registro intermedio, sin traducir jerga ni juzgar otros trabajos; origen = proyecto de portafolio para aprender PINN de papers, no una necesidad de campo. (Guardado en memoria.)
- **Flujogramas complementarios**: §2 termina en "esta data entrena el modelo"; §3 arranca en "datos preprocesados". Sin duplicación.
- **"22/27" en vez de "81.5%"** para claridad (son pozos de entrenamiento).
- Verificado en Docker que **los 30 pozos tienen DCAL** (caliper medido); el fallback `w=1` no se activa.

### Pendientes

- [ ] Activar GitHub Pages (Settings → Pages → Source: carpeta `docs/`).
- [ ] Opcional: "81.5%"→"22/27" en los 6 capítulos de `documentation/`; `hreflang` ES/EN, favicon, README en inglés.
- [ ] Tag `v0.1.0` y verificación de reproducibilidad end-to-end.

### Aprendizajes

- `fetch()` de archivos locales está bloqueado bajo `file://`; para datos en archivo conviene un `.js` (no `.json`). Pero la mejor opción para i18n estático resultó ser dos páginas por idioma.
- mermaid-cli en Chromium headless no tiene las fuentes del sistema → calcula anchos con un fallback. Solución: `htmlLabels:false` (genera `<text>` SVG, no `foreignObject` que no renderiza dentro de `<img>`), fuente métricamente compatible (Helvetica/Arial → Liberation Sans), y `useMaxWidth:false` para tamaño natural y fuente consistente entre diagramas.
