# Bitácora — 2026-05-29

## Sesión: Auditoría completa del preprocesamiento y refactor del pipeline

---

## Contexto

La sesión anterior dejó el pipeline de datos funcionando pero sin una validación rigurosa de calidad. Antes de avanzar a Phase 2, el usuario quiso confirmar que el preprocesamiento era sólido: normalización correcta, tratamiento de outliers justificado, sin riesgo de data leakage en LOWO o en el set externo.

---

## 10:00 — Diagnóstico inicial del estado del preprocesamiento

### Hallazgos

Se revisó `src/preprocessing.py`, `src/data_loader.py` y `tests/test_preprocessing.py` antes de proponer cambios.

**Problemas críticos encontrados:**

1. **NPHI en porcentaje** — Todos los LAS del campo Kraft Prusa almacenan CNLS en % (valores 14–31), no en v/v. El pipeline lo ingería sin corregir → media de NPHI = 36.6 (físicamente imposible en v/v).

2. **Centinelas grandes de resistividad sin detectar** — Herramientas antiguas codifican nulos como 1e9 o 1e11 Ohm·m. Los centinelas clásicos (-9999, -999.25) no los capturan. RT llegaba a 100,000 Ohm·m en el dataset post-limpieza.

3. **Estrategia de clip destructiva** — La función `clip_physical_ranges()` original usaba la misma acción para features y target: NaN a todo lo fuera de rango, luego `dropna(ALL_COLS)`. Esto eliminaba el 100% de las filas de algunos pozos cuando un solo feature tenía problema de escala (ej. Bieberle_Trust_2: SP en escala absoluta 0–666 mV → todas las filas como NaN → pozo vacío).

4. **Estadísticas del EDA eran incorrectas** — El `documentation/01_eda.md` previo tenía NPHI media = 0.267 v/v, cuando el valor sin corregir era 36.6. EDA corrido sobre datos sin corregir.

---

## 11:30 — Correcciones implementadas

### `src/data_loader.py`

**Substep 3.2 — Centinelas de resistividad:**
```python
for res_col in ("RT", "RILM"):
    if res_col in out.columns:
        out.loc[out[res_col].abs() > 1e6, res_col] = np.nan
```

**Substep 3.3 — Conversión NPHI % → v/v:**
```python
if "%" in nphi_unit or (len(nphi_nonnull) > 0 and float(nphi_nonnull.median()) > 1.5):
    out["NPHI"] = out["NPHI"] / 100.0
```
Doble guarda: revisa la unidad del LAS y, si es ambigua, usa la mediana como heurístico.

### `src/preprocessing.py`

Separación de `clip_physical_ranges()` en dos funciones con semánticas distintas:

- **`clip_features_to_bounds(df)`** — Clip al borde para features (GR, RT, RILM, NPHI, SP). Preserva filas. Límites amplios para neutralizar spikes sin descartar litologías legítimas.
- **`filter_invalid_target_rows(df)`** — Drop de filas donde DEN está fuera de (1.5, 3.1) g/cc. El target no puede falsificarse.
- **`clip_physical_ranges()`** — Conservado como wrapper backwards-compatible.

Nuevas constantes:
```python
FEATURE_BOUNDS = {"GR": (0, 400), "RT": (0.05, 50000), "RILM": (0.05, 50000),
                  "NPHI": (-0.15, 0.80), "SP": (-1000, 1000)}
TARGET_BOUNDS = (1.5, 3.1)
```

`preprocess_well()` refactorizado con el orden documentado en EDA:
1. clip_features_to_bounds
2. filter_invalid_target_rows
3. apply_log_rt (si log_rt=True)
4. fit_scaler / scaler.transform

---

## 14:00 — Auditoría end-to-end

Creé `debug/audit_preprocessing.py` que simula exactamente el flujo de entrenamiento:

**Resultado de auditoría (32 pozos):**

| Métrica | Estrategia NaN-todo (anterior) | Clip+filter (nueva) |
|---------|---:|---:|
| Pozos vivos | 30/32 | **32/32** |
| Filas conservadas | 129,661 (75%) | **154,451 (89.5%)** |
| Error inverse transform | 0 | 0 |
| Test wells en [0,1] tras norm | sí | sí |

El pozo Dolecheck_1 tiene NPHI 0.81–5.69 v/v (inconsistencia de unidad en el LAS origin). Sus features se clipean a 0.80; NPHI normalizado queda constante en 1.0. Se mantiene en el dataset pero documentado como caso anómalo para Phase 3.

---

## 15:30 — Actualización de tests

`tests/test_data_loader.py`:
- `test_nphi_percent_unit_converted_to_fraction` — LAS con unit="%"
- `test_nphi_heuristic_converts_when_unit_unknown` — mediana > 1.5
- `test_nphi_vv_unit_not_converted` — mediana 0.25, no toca
- `test_large_rt_sentinel_row_dropped` — RT=1e9, la fila desaparece tras dropna

`tests/test_preprocessing.py`:
- Tests de `clip_features_to_bounds` — spike → boundary, no pierde filas, no toca DEN
- Tests de `filter_invalid_target_rows` — DEN bajo/alto → drop, boundaries exactas OK
- `test_preprocess_well_drops_only_bad_targets` — 110 filas, 1 DEN inválido → 109 filas
- Tests backwards-compat para `clip_physical_ranges`

**Gate final:** 65/65 ✓ · ruff ✓ · mypy ✓

---

## 16:00 — EDA regenerado

- `scripts/02_run_eda.py` actualizado: `plot_per_well_boxplots()`, asimetría en stats, exportación a parquet.
- Figuras regeneradas con datos corregidos (NPHI en v/v, sentinelas eliminados).
- 32 archivos `.parquet` escritos a `data/processed/`.
- `documentation/01_eda.md` reescrito con estrategia completa, tabla de auditoría, caso Dolecheck_1.

---

## Decisiones de diseño

| Decisión | Elección | Por qué |
|----------|---------|---------|
| Features con outliers | Clip al borde | Preserva la fila; el target DEN sigue siendo válido |
| Target DEN con outliers | Drop de fila | No se puede entrenar con DEN=1.0 (washout) o DEN=4.0 (error) |
| NPHI en % | Dividir por 100 si mediana > 1.5 o unit contiene "%" | Heurístico doble; funciona incluso cuando unit="" |
| Centinelas RT | > 1e6 → NaN | Captura los 1e9/1e11 de herramientas antiguas KGS |
| Normalización | Min-max per-well | Confirmado: cada pozo tiene su propio WellScaler sin contaminación cruzada |

---

## Errores cometidos

1. **Asumí que NPHI estaba en v/v sin verificar** — Los histogramas del EDA mostraban media ~36, que es imposible en v/v. Debería haber inspeccionado la unidad del LAS al inicio de Phase 0. Perdí un ciclo de EDA corriendo sobre datos en la escala incorrecta.

2. **No consideré pozos con SP en escala absoluta** — El rango [-200, 100] mV para SP era demasiado estricto. Bieberle_Trust_2 tiene SP 0–666 mV (referencia al nivel del mar). La estrategia NaN universal lo dejaba vacío. Aprendizaje: los bounds de features deben ser loose y la normalización per-well absorbe los offsets absolutos.

---

## Pendientes (sesión)

- [x] Calibrar coeficientes DEN–NPHI: a=-0.090, b=2.265, R²=0.017 (espacio raw; recalibrar en normalizado en Phase 3)
- [x] Verificar acceso a GPU desde el contenedor (hecho en Phase 0)
- [x] Avanzar a Phase 2: `src/model.py`, `src/train.py`, `src/evaluate.py`, LOWO completo

---

## Segunda sesión del día: Phase 2 + estética EDA

### Módulos implementados

**`src/model.py`** — MLP 5→64→64→32→1, dropout opcional, output lineal (sin activación).

**`src/train.py`** — Loop Adam + MSE, early stopping (patience=30), checkpoint del mejor modelo,
`lambda_phys=0.0` ya incluido como stub para Phase 3. `set_seed(42)` aplicado antes de cada fold.

**`src/evaluate.py`** — MAE, RMSE, R², PE_90. Inputs en g/cc (post inverse transform).

**`src/plot_style.py`** — Paleta del proyecto: azul `#2B6CB0` (datos), rojo `#C53030` (regresión/baseline),
verde `#276749` (PINN en Phase 3). `apply_style()` aplica rcParams vía matplotlib.

**`tests/`** — 90 tests en total, todos pasando. Nuevos: `test_model.py` (8), `test_evaluate.py` (9), `test_train.py` (8).

**`scripts/03_train_baseline.py`** — LOWO completo sobre train_pool (29 wells), field_split 3 externos,
inverse transform a g/cc para métricas, guarda predictions parquet y metrics.json.

### Mejoras EDA

Todas las figuras regeneradas con la nueva paleta. Cambios funcionales importantes:
- `distributions_raw.png`: ahora muestra datos post-clip (RT/RILM legibles, mediana/media visibles)
- `log_rt_comparison.png`: rediseñado como 2×2 grid con ejes separados (antes twin-axis = ilegible)
- Crossplots: filtrados a rango físico (Dolecheck_1 NPHI 0.81–5.69 ya no colapsa el eje)

### Documentación

`documentation/02_preprocessing.md` actualizado con 6 figuras insertadas en las secciones correctas:
Fig.1 per-well boxplots, Fig.2 distribuciones, Fig.3 perfil Bieberle_Trust_2,
Fig.4 log RT comparison, Fig.5 crossplot global, Fig.6 crossplot por pozo.

### Estado al cierre

- Entrenamiento LOWO corriendo en background (5/29 folds a las ~21:00)
- ETA: ~1.5h adicionales
- PC se apagará automáticamente al terminar

---

## Pendientes para la próxima sesión

### Inmediato (al arrancar)

- [ ] Revisar `outputs/baseline/metrics.json` — resultados reales del LOWO baseline
- [ ] Escribir `documentation/03_baseline.md` con tabla de métricas por pozo, media±std, discusión
- [ ] Marcar todas las tareas de Phase 2 como `[x]` en `todo/PLAN.md`
- [ ] Hacer `/checkpoint` de Phase 2

### Phase 3 — PINN

- [ ] Calibrar coeficientes DEN–NPHI en **espacio normalizado** (no raw): ajuste lineal sobre datos post-preprocess_well
- [ ] Implementar `src/physics.py`: `den_from_nphi(nphi, a, b)` y `physics_loss(den_pred, nphi_obs, a, b)`
- [ ] Implementar `tests/test_physics.py`: valores tabulados, gradiente, batch
- [ ] Conectar `lambda_phys` en `src/train.py` al physics loss (el stub ya está, solo falta wiring)
- [ ] Implementar `scripts/04_train_pinn.py`: LOWO con λ fijo, guarda a `outputs/pinn/lambda_{λ}/`
- [ ] Implementar `scripts/05_sweep_lambda.py`: λ ∈ {0.0, 0.01, 0.05, 0.1, 0.5, 1.0}
- [ ] Verificar que λ=0 reproduce baseline (tolerancia numérica)
- [ ] Gate de verificación Phase 3: pytest + ruff + mypy
- [ ] Escribir `documentation/03_pinn.md`

### Phase 4 — Resultados y publicación

- [ ] `scripts/06_compare_baseline_vs_pinn.py`: Δ MAE por pozo, tabla pareada
- [ ] `scripts/07_plot_results.py`: curvas DEN real vs baseline vs PINN, λ sweep plot
- [ ] `documentation/04_results.md`
- [ ] `README.md` completo con instrucciones Docker y figura clave
- [ ] Crear repo público `OilCoder/pinn-curvas-sinteticas` y push inicial con tag `v0.1.0`
- [ ] Verificar reproducibilidad end-to-end desde repo clonado

### Deuda técnica menor

- [ ] Evaluar reducir `epochs=300, patience=20` en TrainConfig para acelerar Phase 3 sweep
  (29 folds × 6 lambdas = 174 entrenamientos; con 4 min/fold serían ~11h de cómputo)
- [ ] Añadir `--epochs` y `--patience` como args CLI a los scripts de entrenamiento
