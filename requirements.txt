# torch is NOT listed here — it ships pre-installed with CUDA in the
# pytorch/pytorch base image. Installing from PyPI would overwrite it
# with the CPU-only build.

# Scientific computing
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.14.0

# Gradient boosting (physics-informed XGBoost experiment)
xgboost==2.1.1

# Parquet I/O (for data/processed/*.parquet)
pyarrow==17.0.0

# Well log I/O
lasio==0.31

# Visualization
matplotlib==3.9.1

# Training progress
tqdm==4.66.5

# Linting & type checking
ruff==0.5.5
mypy==1.11.1

# Testing
pytest==8.3.2
pytest-cov==5.0.0
